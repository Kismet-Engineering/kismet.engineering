# kismet.engineering

Single-page Astro site for Kismet Engineering.

## Development

1) Install dependencies: `npm install`
2) Run locally: `npm run dev`
3) Build for production: `npm run build` (output in `dist/`)

## Deployment

GitHub Actions builds the site on pushes to `main` and publishes the `dist/` artifact to GitHub Pages.
