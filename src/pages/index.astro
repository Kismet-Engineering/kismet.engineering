---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Kismet Engineering | Cloud Laboratory">
  <section id="intro" class="hero">
    <div class="hero-text">
      <h1>Building for tomorrow.</h1>
      <p class="lede">
        Kismet Engineering is a cooperative venture focused on bringing reliable, multi-region
        systems within reach. We are shaping a resilient runtime with globally available data
        stores to deliver enterprise-grade resilience from day one.
      </p>
    </div>
    <div class="hero-orbs" aria-hidden="true">
      <span class="orb orb-a"></span>
      <span class="orb orb-b"></span>
      <span class="orb orb-c"></span>
    </div>
  </section>

  <section id="builds" class="builds">
    <div class="section-head">
      <p class="eyebrow">What we build</p>
      <h2>Reliability as the baseline, not the upgrade.</h2>
      <p>Tracks we are advancing to make distributed systems feel predictable and fast.</p>
    </div>
    <div class="card-grid">
      <article class="card">
        <div class="card-head"><span class="dot accent"></span><h3>Distributed runtime</h3></div>
        <p>One deploy path that lands apps where they should, with resilience baked in.</p>
      </article>
      <article class="card">
        <div class="card-head"><span class="dot glow"></span><h3>Programmable data fabric</h3></div>
        <p>Active-active data that balances performance with clarity.</p>
      </article>
      <article class="card">
        <div class="card-head"><span class="dot amber"></span><h3>Automation with humans in the loop</h3></div>
        <p>Operations that explain themselves, stay cost-aware, and keep you in control.</p>
      </article>
    </div>
  </section>

  <section id="principles" class="principles">
      <div class="section-head">
        <p class="eyebrow">How we work</p>
        <h2>Intentional, small, and measurable.</h2>
      </div>
      <div class="principle-grid">
        <div class="principle">
          <h4>Ship proof, not pitch</h4>
          <p>We validate ideas with pilots alongside partners who stress the edges early.</p>
      </div>
      <div class="principle">
        <h4>Reliability as a design input</h4>
        <p>Resilience, observability, and budget safety are part of v1, not backlog items.</p>
      </div>
      <div class="principle">
        <h4>Pragmatic AI</h4>
        <p>We integrate LLM-driven assistance where it clarifies ops and removes toil, never as decoration.</p>
      </div>
    </div>
  </section>

  <section id="contact" class="cta">
    <div class="cta-card">
      <div>
        <p class="eyebrow">Stay in touch</p>
        <h3>We'd love to hear from you!</h3>
      </div>
      <div class="cta-actions">
        <a class="btn primary icon" href="mailto:info@kismet.engineering?subject=Kismet%20Engineering">
          <span class="icon-envelope" aria-hidden="true"><span class="flap"></span></span>
        </a>
      </div>
    </div>
  </section>

  <style>
    .hero {
      position: relative;
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.5rem;
      align-items: start;
      padding: 2.5rem 0 1.25rem;
      overflow: hidden;
    }

    .hero-text {
      max-width: 760px;
      margin: 0 auto;
      text-align: center;
    }

    h1 {
      margin: 0 0 1rem;
      font-size: clamp(2.4rem, 4vw, 3.4rem);
      letter-spacing: -0.02em;
    }

    h2 {
      margin: 0.25rem 0 0.5rem;
      font-size: clamp(1.8rem, 3vw, 2.4rem);
    }

    h3 {
      margin: 0 0 0.35rem;
    }

    h4 {
      margin: 0 0 0.35rem;
      font-size: 1.05rem;
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      padding: 0.35rem 0.7rem;
      border-radius: 12px;
      background: rgba(255, 123, 229, 0.16);
      color: var(--accent);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      font-size: 0.8rem;
    }

    .hero .lede {
      font-size: 1.24rem;
      margin: 0 auto 0.6rem;
      max-width: 820px;
      color: var(--text);
      font-weight: 600;
      padding: 1.1rem 1.4rem;
      border-radius: 16px;
      position: relative;
      background:
        radial-gradient(circle at 20% 20%, rgba(255, 123, 229, 0.16), transparent 55%),
        radial-gradient(circle at 80% 25%, rgba(122, 245, 228, 0.18), transparent 55%),
        linear-gradient(135deg, rgba(122, 245, 228, 0.12), rgba(255, 123, 229, 0.1));
      box-shadow: 0 22px 60px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(255, 255, 255, 0.06);
      text-shadow: 0 4px 16px rgba(0, 0, 0, 0.35);
      overflow: hidden;
    }

    .hero .lede::after {
      content: '';
      position: absolute;
      inset: -40% 10% auto 10%;
      height: 80%;
      pointer-events: none;
      background: radial-gradient(circle at 50% 50%, rgba(255, 255, 255, 0.18), transparent 65%);
      mix-blend-mode: screen;
      opacity: 0.8;
    }

    .cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      margin: 1.2rem 0 1.4rem;
    }

    .dot {
      width: 14px;
      height: 14px;
      border-radius: 50%;
      margin-top: 4px;
      flex-shrink: 0;
    }

    .dot.accent {
      background: var(--accent-2);
      box-shadow: 0 0 12px rgba(122, 245, 228, 0.45);
    }

    .dot.glow {
      background: #6ea2ff;
      box-shadow: 0 0 12px rgba(110, 162, 255, 0.45);
    }

    .dot.amber {
      background: var(--accent);
      box-shadow: 0 0 12px rgba(255, 123, 229, 0.45);
    }

    .focus-card {
      border: 1px solid var(--border);
      background: linear-gradient(185deg, rgba(17, 36, 72, 0.95), rgba(14, 24, 48, 0.78));
      border-radius: 18px;
      padding: 1.2rem 1.3rem;
      box-shadow: var(--shadow);
    }

    .focus-card ul {
      list-style: none;
      padding: 0;
      margin: 0.6rem 0 0;
      display: grid;
      gap: 0.65rem;
    }

    .focus-card li {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 0.6rem;
      align-items: start;
      padding: 0.55rem 0.6rem;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid var(--border);
    }

    .focus-card strong {
      color: white;
    }

    .focus-card small {
      display: block;
      color: var(--muted);
      margin-top: 0.05rem;
    }

    .badge {
      display: inline-block;
      padding: 0.3rem 0.65rem;
      border-radius: 10px;
      background: rgba(244, 211, 94, 0.18);
      color: var(--accent);
      font-weight: 700;
      letter-spacing: 0.02em;
      text-transform: uppercase;
      font-size: 0.75rem;
      margin-bottom: 0.5rem;
    }

    .builds {
      margin: 3rem 0 2rem;
    }

    .section-head {
      max-width: 760px;
      margin-bottom: 1.2rem;
    }

    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1rem;
    }

    .card {
      padding: 1.1rem;
      border-radius: 16px;
      border: 1px solid var(--border);
      background: rgba(255, 255, 255, 0.03);
      box-shadow: var(--shadow);
    }

    .card-head {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.3rem;
    }

    .card ul {
      margin: 0.75rem 0 0;
      padding-left: 1.1rem;
      color: var(--muted);
    }

    .principles {
      margin: 2rem 0 3rem;
    }

    .principle-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 0.9rem;
    }

    .principle {
      padding: 1rem;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: rgba(12, 22, 42, 0.8);
    }

    .cta {
      margin: 3rem 0 2rem;
    }

    .cta-card {
      border: 1px solid var(--border);
      background: linear-gradient(120deg, rgba(12, 22, 42, 0.95), rgba(15, 31, 57, 0.9));
      border-radius: 18px;
      padding: 1.4rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      box-shadow: var(--shadow);
      flex-wrap: wrap;
    }

    .cta-actions {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
    }

    .btn.icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 86px;
      height: 68px;
      padding: 0;
      border-radius: 16px;
    }

    .icon-envelope {
      display: inline-block;
      width: 42px;
      height: 28px;
      background: linear-gradient(135deg, rgba(255, 123, 229, 0.98), rgba(255, 160, 240, 0.9));
      border-radius: 6px;
      position: relative;
      box-shadow: 0 14px 28px rgba(255, 123, 229, 0.4);
      border: 1.5px solid rgba(255, 255, 255, 0.14);
    }

    .icon-envelope::before,
    .icon-envelope::after {
      content: '';
      position: absolute;
      left: 0;
      right: 0;
      height: 2px;
      background: rgba(255, 255, 255, 0.45);
      border-radius: 999px;
    }

    .icon-envelope::before {
      top: 50%;
      transform: translateY(-2px) skewY(-15deg);
    }

    .icon-envelope::after {
      top: 50%;
      transform: translateY(-2px) skewY(15deg);
    }

    .icon-envelope .flap {
      position: absolute;
      inset: -1px;
      background: linear-gradient(155deg, rgba(255, 175, 242, 0.4), rgba(255, 123, 229, 0.2));
      clip-path: polygon(0 0, 100% 0, 50% 65%);
      border-radius: 6px 6px 2px 2px;
      box-shadow: inset 0 -6px 10px rgba(255, 255, 255, 0.12);
    }

    .hero-orbs {
      position: absolute;
      inset: 0;
      pointer-events: none;
      overflow: hidden;
    }

    .orb {
      position: absolute;
      filter: blur(28px);
      opacity: 0.55;
      border-radius: 50%;
      mix-blend-mode: screen;
    }

    .orb-a {
      width: 240px;
      height: 240px;
      background: radial-gradient(circle, rgba(255, 123, 229, 0.35), transparent 55%);
      top: -40px;
      right: 8%;
    }

    .orb-b {
      width: 180px;
      height: 180px;
      background: radial-gradient(circle, rgba(122, 245, 228, 0.28), transparent 60%);
      bottom: -20px;
      left: 12%;
    }

    .orb-c {
      width: 280px;
      height: 280px;
      background: radial-gradient(circle, rgba(110, 162, 255, 0.26), transparent 60%);
      top: 20%;
      left: 45%;
    }

    @media (max-width: 840px) {
      .nav-links .btn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</BaseLayout>
